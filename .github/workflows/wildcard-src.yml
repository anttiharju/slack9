permissions: {}
on:
  push:
    branches:
      - wildcard
    paths:
      - "src/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-src.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
