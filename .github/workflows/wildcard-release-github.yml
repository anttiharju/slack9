permissions: {}
on:
  push:
    branches:
      - wildcard
    paths:
      - ".release/archive.sh"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-release-github.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
