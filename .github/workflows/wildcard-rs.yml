permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - ".github/workflows/wildcard-rs.yml"
      - "**/*.rs"
      - "Cargo.lock"
      - "Cargo.toml"
      - "flake.lock"
      - "flake.nix"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
