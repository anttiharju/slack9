permissions:
  contents: none
  pull-requests: none
on:
  push:
    branches:
      - wildcard
    paths:
      - "**.rs"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/wildcard-rs.yml"
jobs:
  wildcard:
    runs-on: ubuntu-latest
    steps:
      - run: |
          true
